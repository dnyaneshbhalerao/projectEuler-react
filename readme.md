# Practice repository to learn react-ecosystem

  ## 1. Install required dependancies and packages
    `npm run install`

  ## 2. Build the project
    `npm run build`

  ## 3. Run the Server
    `npm run start`